# Railway config — deploys the Dockerfile and mounts a persistent volume for the database.
[build]
builder = "dockerfile"

[deploy]
startCommand = "node server.js"
restartPolicyType = "on_failure"

# Attach a volume: right-click the project canvas → Volume (or ⌘K → "Create Volume"),
# attach it to this service, mount path: /data
